# Local (no docker) development overrides
LOCAL_DEV=true
SQLALCHEMY_DATABASE_URL=sqlite:///./local_dev.db
# Optionally set SUPABASE_URL / API_ISSUER if you want to test real auth; leave blank to use stub user.
SUPABASE_URL=
API_ISSUER=
